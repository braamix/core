// <math.h> for the vendored sources: the library's own declarations, plus the
// C-only spellings musl uses internally. Programs include "math/math.h".
#pragma once

#include <features.h>
#include <stdint.h>

#include "../../math.h"

typedef double double_t;
typedef float float_t;
